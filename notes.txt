
# Feedback notes
1) V Done!  make [int], ['name'] more robust, return None with warning if not found
  rather than throwing an error

2) V Done! enable adding reports : report_01 + report_02 = report_03

3) V check if possible to filter low similarity matches, e.g. < 0.05

4) Report class: add option to get 'file1'/'file2' set

5) check option to compare inside zip files

6) V (relative) Check if complete is relative or binary

7) V if relative, sort/filter by complete

8) check the override policy

9) Report.save: if the given file name has no extension, and the parent exist,
   add '.pkl' instead of creating a dir

10) V add option for to record only if there is  a minimum similarity

11) add option to search few dirs as a list or pattern

12) make Report from_dir accept a list of dirs or a pattern

13) don't throw error if no files/dir found, just print warning

14) if the same path (file or dir)  appears twice in file1 list, ignore the 2nd  one


***********************
* file_actions.py
***********************
1) add ':' operator to fix_numbering

2) in move_files, switch 1st and 2nd arg to be more intuitive: move_files(src, dst) rather than move_files(dst, src)


***
Traceback (most recent call last):
  File "C:\Program Files\JetBrains\PyCharm Community Edition 2024.1.4\plugins\python-ce\helpers\pydev\pydevconsole.py", line 364, in runcode
    coro = func()
           ^^^^^^
  File "<input>", line 1, in <module>
  File "E:\Xos\Python\Projects\my_py_utils\compare_report.py", line 59, in __getitem__
    return ReportView(self._rows[key])
                      ~~~~~~~~~~^^^^^
TypeError: 'set' object is not subscriptabl
